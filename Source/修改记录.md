# 修改记录

> 规则：每次修改后追加一条记录（修改时间精确到秒、修改人、修改文件完整地址/名称、修改内容）。最新记录在最下方。

| 修改时间 | 修改人 | 修改文件（完整地址/名称） | 修改内容 |
|----------|--------|--------------------------|----------|
| 2026-09-11 14:44:00 | Tony | `D:\Study\AICode\WindowsEXE\.agent\AGENTS.md` | 依据 `DOCS\` 下 01 设计方案与 02 详细设计说明书，新建项目 AGENTS.md 初版（项目概览/构建测试/目录结构/代码规范/架构说明/通用规则/记忆库说明） |
| 2026-09-11 14:52:00 | Tony | `D:\Study\AICode\WindowsEXE\.agent\AGENTS.md` | 依据 `DOCS\02Windows_Nova_Launcher_V1.0_极简方案.md` 重写：技术栈改为 C#/.NET 10/WinUI 3/System.Text.Json/Windows API；存储改为 apps.json+settings.json；目录扁平化单项目；架构改为 UI→AppService→JSON；详细说明书降级为 V2.0 演进蓝图 |
| 2026-09-11 14:55:00 | Tony | `D:\Study\AICode\WindowsEXE\.agent\AGENTS.md` | 读取 Obsidian 记忆库开发偏好，新增「开发与协作偏好」段落（Git 提交规范、临时文件 .traetemp 不入库、本机环境注意、UI 验证闭环） |
| 2026-09-11 14:56:39 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\03Windows_Nova_Launcher_V1.0_极简方案开发计划.md` | 依据 02 极简方案新建 V1.0 详细开发计划（目标与范围 IN/OUT、技术选型、架构与数据契约、9 阶段 M0–M8、WBS 任务清单≈8 人天、UI/动画规范、验收标准、风险对策、交付物清单） |
| 2026-09-11 15:04:08 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\03Windows_Nova_Launcher_V1.0_极简方案开发计划.md` | 将打包方式改为**绿色版**：技术选型「打包」→「绿色版（非打包+自包含）」；M0 采用非打包模式；M8 重写为「绿色版发布与验收」（新增 csproj 配置与 `dotnet publish` 命令、T8.1–T8.5）；WBS、交付物清单、里程碑看板同步由 MSIX 安装包改为 `NovaLauncher-win-x64.zip` |
| 2026-09-11 15:16:47 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\prototype\nova-launcher.html` | 新建 Nova Launcher UI 原型（单文件自包含）：实现极简方案 V1.0 全部界面与交互（添加/移除、拖拽排序、图标大小 36~160、每行图标数 4~8、深浅主题、Mica 渐变背景、Hover 放大/玻璃卡片、抽屉滑入、启动提示），数据用 localStorage 模拟 apps.json/settings.json |
| 2026-09-11 15:16:47 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260911-原型验证\` | agent-browser 截图验证 6 张（深色：首页/悬停/设置抽屉/大图标128；浅色：首页/设置抽屉） |
| 2026-09-11 15:16:47 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\原型验证报告.md` | 新建原型验证报告（环境限制说明、功能覆盖、7 项功能测试全部通过、截图证据清单） |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **新建**「HTML + 本地宿主」方案的核心宿主（515 行，UTF-8 **带 BOM**）：`HttpListener` 监听 127.0.0.1 随机端口并托管前端页面；`PrivateExtractIcons` P/Invoke 提取 256px 真实图标并按路径 MD5 缓存；`/api/state|icon|launch|pick|add|remove|move|setting|reveal|quit` 接口（全部校验随机 token）；3 秒心跳 + 25 秒无心跳自动退出；日志写 `%LOCALAPPDATA%\NovaLauncher\host.log` |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.bat` | **新建**绿色版入口（29 行，UTF-8 无 BOM + **CRLF**）：双击以 Hidden 方式拉起宿主，宿主再以 `msedge --app` 打开无地址栏窗口 |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **新建**界面层（745 行）：承接原原型全部 UI/交互，改接宿主同源 API（真实图标、真实启动、真实持久化）；新增空状态引导、路径失效红标、Esc 关闭抽屉、演示模式回退（直接双击 HTML 时不启动任何程序） |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 新建使用说明：用法、文件职责与编码要求、数据目录、工作原理图、直接打开 HTML 的演示模式、常见问题 |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\tools\capture-window.ps1`、`ui-drive.ps1` | 新建验证工具：窗口截图（DPI 感知）与窗口驱动（置顶 + SendKeys + 客户端坐标点击），均按进程名/标题/PID 精确匹配窗口 |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260911-HTML宿主方案\` | 截图 4 张：真实窗口（深色，8 个真实图标）、设置抽屉（深色）、首页（浅色 + 大图标 120px）、设置抽屉（浅色） |
| 2026-09-11 16:40:34 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\HTML宿主方案验证报告.md` | 新建验证报告：方案选型对比、交付物、接口层 7 项实测、**真实启动程序日志实录**、截图证据、**5 个真实缺陷根因与修复**、未验证边界、与原 WinUI 3 开发计划的关系说明 |
| 2026-09-12 13:58:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M13 每行图标数扩展到 12**：`#colsSel` 由 4~8 扩到 4~12（新增 9/10/11/12）；网格列宽由固定 `calc(var(--icon-size) + 40px)` 改为 `minmax(0, calc(...))` 实现**自适应收缩**（原公式下 12 列 × 104px 图标需 1728px，远超 `--window-size=1180` 的可用宽度，必然横向溢出）；图标盒改 `width:min(100%, var(--icon-size))` + `aspect-ratio:1`，加 `container-type:inline-size` 与字形字号 `42cqw`，列宽被压缩时图标与字形兜底同步缩小不互相压盖 |
| 2026-09-12 13:58:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | `/api/setting` 的 `cols` 夹取上限由 `[Math]::Min(10, $n)` 提到 `Min(12, $n)`——否则前端选 12 会被服务端**静默压回 10**（前端已显示 12、落盘却是 10） |
| 2026-09-12 13:58:00 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260912-每行12个\` | 截图 3 张：`01-主页-每行12个.png`（深色，首行实测 12 个）、`02-设置-每行图标数.png`（外观面板显示「每行图标数 = 12」）、`03-浅色主题-12列.png` |
| 2026-09-12 13:58:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M13 里程碑**（T13.1–T13.5，含自适应列宽的取舍说明与实测数据）、WBS 汇总表与里程碑看板同步、顶部状态行补 M13 |
| 2026-09-12 13:58:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 用法补「每行图标数 4~12 + 列宽会自动等比压缩（图标跟着变小）、12 列下想不缩图标就把图标调小」；顺带修正列表序号重复（两个 6.）|
| 2026-09-12 14:22:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M14 拖拽排序落盘修复**：`/api/move` 里的 `$apps = @(Get-Apps)` 改为 `$apps = Get-Apps` ＋ `$n = @($apps).Count`。`@()` 包裹**返回数组**的函数调用得到的是「1 个元素」的数组（`.Count` 恒为 1），导致校验 `$to -ge 1` 一律成立 → 每次拖拽都被判「索引越界」→ 排序从上线起从未真正写入 `apps.json`；并把该陷阱写进代码注释 |
| 2026-09-12 14:22:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M14 图标与名字强绑定**：`API.iconUrl` 由按行号 `/api/icon?i=N` 改为**按路径** `/api/icon?p=<path>`（`shell:` 应用返回 null 直接走字形兜底）——行号随排序变化，一旦界面顺序与落盘顺序分叉，按行号取图必然与名字整体错位；`dragend` 增加 `ok === false` 判定，失败时 `refresh()` 拉回宿主真实顺序 + toast 报错，不再「界面重排而落盘没变」 |
| 2026-09-12 14:22:00 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260912-拖拽排序\` | 截图 3 张：`01-拖拽前.png`、`02-拖拽后（第1格已到第4位）.png`、`03-刷新后-顺序已落盘.png` |
| 2026-09-12 14:22:00 | Tony | `D:\Study\AICode\WindowsEXE\.temp\ui\` | 新增验证脚本：`chk.js`（把每个磁贴的图标画进 canvas 取哈希，与「该磁贴名字对应应用的图标」比对，输出 `window.__chk`）、`sum.js`、`ord.js`/`ordsum.js`（比对界面顺序／前端内存顺序／图标来源路径／dataset.index）、`synth.js`（合成真实鼠标式拖拽：dragstart → 沿直线 10 帧 dragover → drop → dragend）、`restore-order.js`（按路径精确还原应用顺序） |
| 2026-09-12 14:22:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M14 里程碑**（T14.1–T14.6）、WBS 汇总与看板同步、风险表新增「`@()` 包裹返回数组的函数」与「资源按行号寻址」两条 |
| 2026-09-12 14:22:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 用法补「拖拽后松手即写入 apps.json，保存失败会提示并拉回真实顺序；图标按路径读取，永远与名字一一对应」 |
| 2026-09-12 14:26:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M15 屏蔽主界面右键菜单**：新增 `isEditableTarget()` + `document` 上 **capture 阶段**的 `contextmenu` 监听，`preventDefault()` ＋ `stopPropagation()`（capture 阶段即可截断，无需等冒泡到目标）。**放行可编辑文本控件**（text/search/url/email/password/tel/number/textarea/contenteditable）——否则搜索框右键粘贴、拼写检查一同失效；右键菜单是浏览器原生 UI、不在 DOM 中，Edge 无对应启动开关，只能 JS 拦 |
| 2026-09-12 14:26:00 | Tony | `D:\Study\AICode\WindowsEXE\.temp\ui\ctx.js` | 新增验证脚本：在 `document` 挂**最末尾**的冒泡监听读取 `defaultPrevented`，对磁贴／网格／角标／搜索框各合成一次 `contextmenu`（`cancelable:true`）并回读拦截结果；注意被屏蔽的事件因 `stopPropagation` 不会冒泡到 document，故冒泡监听只会收到「被放行」的那条，这本身即是拦截生效的旁证 |
| 2026-09-12 14:26:00 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260912-右键屏蔽\` | 截图 1 张：`01-右键屏蔽验证.png`（深色主界面 19 个应用，界面无异常） |
| 2026-09-12 14:26:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M15 里程碑**（T15.1–T15.4）、WBS 汇总与看板同步、顶部状态行补 M15；风险表新增「右键菜单是浏览器原生 UI、无法用启动参数关闭」与「capture 阶段直接放行可编辑控件」两条 |
| 2026-09-12 15:58:00 | Tony | `D:/Study/AICode/WindowsEXE/NovaLauncher.Web/NovaLauncher.ps1` | **M16 无边框全屏 + 窗口三键（宿主侧）**：新增 `NovaWindow` P/Invoke 类（`GetWindowLong`/`SetWindowLong(GWL_STYLE)`、`SetWindowPos`、`IsWindow`/`IsWindowVisible`/`IsIconic`、`GetWindowThreadProcessId`、`EnumWindows`、`GetForegroundWindow`、`ShowWindow`、`PostMessage(WM_CLOSE)`）；新增 `Get-NovaWin`（`EnumWindows` 按**标题 + 浏览器进程名白名单 msedge/chrome** 匹配，不用 `$proc.MainWindowHandle`——Edge 常把新窗口交给已存在的浏览器进程，启动进程会立刻退出导致句柄恒为 0；同名多窗口时优先取**当前前台**那个）、`Get-NovaWinState`（`minimized`/`fullscreen`/`windowed`）、`Set-NovaWinFullscreen`（去 `WS_CAPTION|WS_THICKFRAME` 后 `SetWindowPos` 铺满屏幕，**必带 `SWP_FRAMECHANGED`** 否则样式不生效）、`Invoke-StartupFullscreen`（**惰性应用**全屏，`$script:FsApplied` 保证只切一次）；新增 `'^/api/win$'` 路由（`op=min/max/close/state`）；启动参数改为 `--window-size=<主屏宽>,<主屏高>`，不再在启动阶段死等窗口 |
| 2026-09-12 15:58:00 | Tony | `D:/Study/AICode/WindowsEXE/NovaLauncher.Web/nova-launcher.html` | **M16 无边框全屏 + 窗口三键（前端侧）**：标题栏右侧新增 `.wctrl` 三键 `#winMin`/`#winMax`/`#winClose`（SVG 线条图标，`body:not(.hosted)` 时整组隐藏）；新增 `API.win(op)` 走同源 `/api/win`；`#winMax` 按 `state` 在「最大化/还原」两个 SVG 间切换；加载后先拉一次 `op=state` 同步按钮图标；启动时窗口按主屏尺寸开、随后由宿主切成无边框全屏 |
| 2026-09-12 16:02:00 | Tony | `D:/Study/AICode/WindowsEXE/NovaLauncher.Web/nova-launcher.html` | **M17 精简主界面文案**：删除主区 `.hero` 区块（`<h1>我的应用</h1>` + 说明行「拖拽图标排序 · 点击启动 · 也可把程序直接拖进窗口添加」）；删除主区底部 `<div class="hint" id="hint">` 及其 JS 写入逻辑（原「共 N 个应用 · 顺序已保存」+「N 个路径失效」），`hintEl` 变量同步移除；清掉 `.hero` 的 CSS 规则，`.main` 顶距 `6px`→`18px` 补回呼吸位；空状态（0 应用时才有）压成一行，去掉三段操作说明。**主界面现在只有图标和名字**；失效应用仍由磁贴自身标红（`.tile.missing`）呈现，信息不丢 |
| 2026-09-12 16:02:00 | Tony | `D:/Study/AICode/WindowsEXE/.temp/ui/clean.js` | 新增验证脚本：检查 `.hero`/`#hint` 是否已移除、主区可见文本、禁用词命中（我的应用／个应用／顺序已保存／拖拽图标排序／路径失效／点击右上角设置）、磁贴数、标签杂质、首个磁贴距主区顶距离、设置抽屉存在性 |
| 2026-09-12 16:02:00 | Tony | `D:/Study/AICode/WindowsEXE/.traetemp/截图/20260912-精简主界面/` | 截图 1 张：`01-主界面无标题文案.png`（主界面仅剩标题栏 + 图标网格，19 个应用） |
| 2026-09-12 16:04:00 | Tony | `D:/Study/AICode/WindowsEXE/DOCS/04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M16 里程碑**（T16.1–T16.7 无边框全屏 + 窗口三键）与 **M17 里程碑**（T17.1–T17.5 精简主界面文案）；WBS 汇总与里程碑看板同步；顶部状态行补 M16/M17；风险表新增 6 条（网页无法直接控制浏览器窗口／`--start-fullscreen`·`--kiosk` 不可用／`SetWindowLong` 后需 `SWP_FRAMECHANGED`／`MainWindowHandle` 拿不到句柄／启动阶段死等窗口会卡住 HTTP 服务／主界面文案与真实状态可能不同步）；交付物清单体积同步更新 |
| 2026-09-12 16:05:00 | Tony | `D:/Study/AICode/WindowsEXE/NovaLauncher.Web/README.md` | 「怎么用」第 2 步改为「窗口自动铺满屏幕 + 右上角三键」；「界面细节」新增「主界面只有图标和名字」与「无边框全屏 + 右上角窗口三键」两条；「工作原理」补 `/api/win` 窗口控制说明（`EnumWindows` 匹配、`SWP_FRAMECHANGED`、惰性全屏） |
| 2026-09-12 16:13:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M18 连接状态改为圆点指示**：标题栏角标由文字「已连接宿主」改为 `<i class="dot">` + `<span class="badge-txt">` 结构，用 `data-state`（`pending`/`online`/`offline`）驱动——**绿点＝已连接且不出文字**、**红点＝未连接并显示「未连接宿主」**＋药丸红底＋`conn-pulse` 呼吸动画、灰点＝连接中；主题变量新增 `--ok`/`--ok-glow`/`--danger-glow`（深色 `#57d98a`、浅色 `#1aa35a`；圆点 11px ＋ 4px 光晕）；新增 `setConn(state,title)` 单一状态入口，`init()` 三处分支与 `title` 提示统一走它；心跳 `API.ping()` 由 fire-and-forget 改为按响应回调 `connOk`/`connFail`，**连续失败 2 次**才翻红，从而能发现「宿主中途崩掉」 |
| 2026-09-12 16:13:00 | Tony | `D:\Study\AICode\WindowsEXE\.temp\ui\conn.js` | 新增验证脚本：回读 `data-state`、圆点 `backgroundColor` 与 `boxShadow`、`animationName`、文字 `display` 与内容、角标可见文本与 `title`、`body.hosted`、协议，并按 `rgb` 值判定绿/红 |
| 2026-09-12 16:13:00 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260912-连接状态指示\` | 截图 3 张：`01-已连接-绿点无文字.png`（4 倍放大，可见绿点与光晕、无任何文字）、`02-未连接-红点带提示.png`（停宿主后自动翻红）、`03-演示模式-红点带提示.png` |
| 2026-09-12 16:13:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M18 里程碑**（T18.1–T18.6）、WBS 汇总与看板同步、顶部状态行补 M18；风险表新增 2 条（状态用文字表达必占位且颜色/文字易走岔；心跳只发不收导致宿主崩了页面不知情）；M17 的「未改动项」说明同步为「连接状态指示保留、显示形式于 M18 改为圆点」；交付物清单体积更新 |
| 2026-09-12 16:13:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 「界面细节」新增「连接状态是一个圆点，不是一个词」；「直接打开 HTML 会怎样」改为「状态点为红色并提示未连接宿主、窗口三键自动隐藏」 |
| 2026-09-12 16:13:00 | Tony | `C:\Users\tonyy\.workbuddy\skills\windows-html-native-launcher\SKILL.md` | 新增 §13「连接状态用状态点而不是文字」（`data-state` 单源驱动颜色与文字显隐、正常态不出文字、断连才出文字 + 呼吸动画、圆点尺寸要按高分屏校验、心跳兼作存活探测且需连续失败阈值） |
| 2026-09-12 18:03:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M19 最大化（非全屏）+ 隐藏浏览器自绘标题栏**：① 全量更名 `Set-NovaWinFullscreen`→`Set-NovaWinMaximized`、`Invoke-StartupFullscreen`→`Invoke-StartupMaximize`、`$script:FsApplied`→`$script:MaxApplied`、状态值 `'fullscreen'`→`'maximized'`（逐个替换并 grep 复核）；② `Set-NovaWinMaximized` 改用 `Screen.WorkingArea`（而非 `Bounds`）铺满，任务栏保持可见；③ **新增 `Update-NovaCsdHeight()`**：用页面回报的 `window.innerHeight` 反推 Chromium 自绘标题栏高度（`客户区高 − innerHeight × 尺度系数`，系数由 `IsProcessDPIAware()` 决定取 `dpr` 还是 1），算不出合理值（负数／>200）时**放弃偏移**；④ `Set-NovaWinMaximized` 按该高度把窗口整体**上移**并同步加高（底边不动）→ 那条自绘栏被顶出屏幕上边缘；⑤ `NovaWindow` 类新增 `GetClientRect`/`RECT`/`IsProcessDPIAware`；⑥ `/api/ping`、`/api/state` 接收 `ih`/`dpr`，`/api/win?op=state` 回包增加 `bar` 便于排查；⑦ **修真 bug**：`Start-Process -ArgumentList` 由**数组**改为**整条命令行字符串**——PS 5.1 会把 `'--user-data-dir=' + 路径` 拆成两段（`=` 后多一个空格），浏览器收到空值后**退回用户自己的 Chrome 配置**，`BrowserProfile` 建不出来 |
| 2026-09-12 18:03:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M19 前端配合**：`API.state()` 增加 `ih: window.innerHeight` / `dpr: window.devicePixelRatio` 上报；心跳 `API.ping()` 改走 `encodeQuery` 并附带同样两个字段（宿主据此反推自绘标题栏高度）；`applyWinChrome` 的状态判断由 `"fullscreen"` 改为 `"maximized"`（初始化调用同步） |
| 2026-09-12 18:03:00 | Tony | `D:\Study\AICode\WindowsEXE\.temp\csdtest.ps1`、`verify-live.ps1`、`screen-cap.ps1`、`fullcap.ps1`、`cleanup.ps1` | 新增/更新验证与清场工具：`csdtest.ps1`（对比 `--disable-windows10-custom-titlebar` 等方案下的窗口外观并逐行扫描自绘栏高度）、`verify-live.ps1`（查 `/api/win?op=state` ＋ 量窗口矩形/客户端区 ＋ `PrintWindow` 抓窗口并按列扫描）、`screen-cap.ps1`/`fullcap.ps1`（`BitBlt` 抓屏幕顶边/整屏）、`cleanup.ps1`（按 `ApiToken novatestNN` 精确杀残留宿主 + 关闭同名窗口） |
| 2026-09-12 18:03:00 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260912-最大化去标题栏\` | 实测证据 5 件：`01-窗口实拍-浏览器自绘栏占窗口0至43行.png`（`PrintWindow` 抓窗口）、`02-改造前对比-自绘栏正好落在屏幕可见区.png`、`03-窗口几何与自绘栏边界-关键数据.txt`（窗口 `0,-44 2561×1412`、底边 1368 = 工作区底边、自绘栏映射到屏幕 y=−44..−1）、`04-抓屏能力探测日志.txt`、`05-聚焦后抓取日志.txt`；`/api/win?op=state` → `{state:"maximized", bar:29}`；`max` ⇄ `maximized` 往返后偏移保持；会话前半段整屏实拍已直接确认「顶部只有 NOVA 栏、底部任务栏可见」，后半段本机 `BitBlt` 抓屏失效（8/8 返回 False）故以窗口几何归档 |
| 2026-09-12 18:03:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M19 里程碑**（T19.1–T19.7：最大化工序、自绘标题栏的发现与上移方案、DPI 尺度陷阱、接口扩展、`--user-data-dir` 修复、回归验证）；WBS 汇总与里程碑看板同步；M16 节加「后续调整（见 M19）」提示；风险表新增 3 条（去 `WS_CAPTION` 后仍有 Chromium 自绘栏、反推栏高混算 DPI 尺度、PS 5.1 `-ArgumentList` 数组拆参致配置隔离失效），并订正「铺满屏幕」为「铺满工作区」 |
| 2026-09-12 18:03:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 「怎么用」第 2 步与「界面细节」改为「无边框**最大化**（铺满工作区、任务栏可见）、顶部只剩页面自己的 NOVA 栏、系统栏与浏览器自绘栏都已去掉」；「工作原理」补 `WorkingArea` 与「浏览器自绘标题栏如何被顶出屏幕、栏高如何反推」；数据目录说明订正为「不影响日常 Edge/Chrome」 |
| 2026-09-12 18:03:00 | Tony | `C:\Users\tonyy\.workbuddy\skills\windows-html-native-launcher\SKILL.md` | 新增 §14「Chromium `--app` 窗口的『头条标题栏』要去两层」：① Win32 去 `WS_CAPTION`；② Chromium 自绘的 custom titlebar 无开关可关（含 `--disable-windows10-custom-titlebar` 实测无效、`--kiosk` 的坑），只能把窗口整体上移该栏高度；栏高按 `客户区高 − innerHeight×尺度系数` 反推，系数取决于 `IsProcessDPIAware()`；并新增「PS 5.1 `Start-Process -ArgumentList` 传数组会把 `--flag=值` 拆成两段」的踩坑条目 |
| 2026-09-12 18:35:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M20 宿主侧**：① 新增 `Bring-NewWindowToFront()` —— 启动前后**快照顶层窗口取差集**找到本次拉起的新窗口（不按进程找：`.lnk` 经 shell 中转、UWP 经 explorer 拉起，`-PassThru` 的进程常常不拥有窗口），目标优先「进程是被启动的那个」、同档取面积最大，命中后再观察 0.7s 让主窗口取代闪屏；② `NovaWindow` 类新增 `ForceForeground()`（分两层：`SetWindowPos(HWND_TOPMOST→NOTOPMOST)` 抬 z 序**不需要权限、必然生效**；键盘焦点再依次用 `PeekMessage` 建消息队列 → `AttachThreadInput` → 临时清零 `SPI_SETFOREGROUNDLOCKTIMEOUT`（用完还原）→ 轻敲 ALT）与 `SnapshotTopLevel/TitleOf/PidOf/ProcNameOf/ZOrderIndexOf`；③ 新增 `Resolve-AppRef()`：浏览器不给拖入文件的绝对路径，改为按**文件名**在 用户桌面/公共桌面/开始菜单(递归)/Programs/快速启动/下载 建索引解析（缓存 60s，命中优先 `.lnk`，可退一步按基名匹配），外层包 try/catch 保证解析异常不产生 500；④ `Get-ShortcutSearchRoots()` 改用 `[Environment]::GetFolderPath()` + 新增 `Join-Safe()` —— **修掉一个 500 bug**：本机 `$env:APPDATA`/`$env:USERPROFILE` 为空，`Join-Path` 抛「无法将参数绑定到参数"Path"，因为该参数是空值」；⑤ `/api/addbatch`、`/api/add` 接入解析（仅当原路径不可用时才解析，避免误清 `kind`）；⑥ `/api/reveal` 支持 `i`/`p`，用 `explorer /select,"路径"` 定位选中快捷方式；⑦ 类注释补记「PS 5.1 的 `Add-Type` 是老编译器，不支持 C# 7 内联 out 声明」 |
| 2026-09-12 18:35:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M20 前端侧**：① 新增**图标右键菜单**（`.ctxmenu` 样式 + `#ctxmenu` 容器 + `showCtx/buildCtx/positionCtx/closeCtx`）：标题行显示应用名，三项目「打开 / 打开文件位置 / 移除快捷方式」，移除做成**菜单原地二次确认**（第一次点变「再点一次确认移除」），贴边自动翻边，点外部/滚轮/失焦/Esc 自动收起；⚠ 触发**并入既有的 capture 阶段 `contextmenu` 处理器**（原处理器有 `stopPropagation`，冒泡阶段挂磁贴监听器收不到事件），并放行菜单自身与可编辑控件；② `API.reveal(i)` 支持传索引，新增 `revealApp()`（商店应用明确提示无可定位文件）；③ 拖入改为 `fileRefsFrom()` 上报 **路径+文件名**（`p`/`n`），`addPaths()` 直接提交 refs 并在单条成功时回显应用名；④ 拖入遮罩文案改为「把快捷方式或程序拖到这里即可添加」并说明可从开始菜单拖；⑤ 磁贴 `title` 补「右键：移除快捷方式 / 打开文件位置」提示 |
| 2026-09-12 18:35:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M20 里程碑**（T20.1–T20.11：差集找新窗口、z 序 vs 键盘焦点两层置前、目标选取与收尾、图标右键菜单、capture 阶段 stopPropagation 陷阱、`/api/reveal` 扩展、拖入按名称解析、解析兜底与诚实失败、`$env:APPDATA` 空值 500 修复、宿主侧与页面侧回归验证）；WBS 汇总与里程碑看板同步；风险表新增 8 条（后台进程拉起的窗口拿不到前台资格、按进程找窗口不可靠、浏览器不暴露拖入文件绝对路径、`Join-Path $env:APPDATA` 空值 500、capture 阶段拦截阻断子元素监听器、`Add-Type` 老编译器不支持 C#7、移除操作误点、`agent-browser` 会连带清掉已有浏览器进程致宿主自退）；交付物清单更新文件体积与内容说明 |
| 2026-09-12 18:35:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 「怎么用」补充：启动的程序会**自动显示到最前**；添加方式增列「拖入快捷方式 / 从开始菜单拖」与「右键 → 打开文件位置」；新增第 7 条**图标右键菜单**说明（含「只删图标记录、不动磁盘文件」与二次确认）；「界面细节」把「右键菜单已屏蔽」改为「图标上是自己的菜单、其他地方一律屏蔽」并标注 capture 阶段陷阱；「工作原理」新增两段（置前为什么要分 z 序与焦点两层、拖入快捷方式如何按名称找回真实路径及为何不能用 `$env:APPDATA`）；FAQ 新增 3 条（拖入提示未找到 / 程序没到最前 / 右键菜单没出来） |
| 2026-09-12 18:35:00 | Tony | `D:\Study\AICode\WindowsEXE\.traetemp\截图\20260912-右键菜单与拖入添加\` | 实测证据 4 件：`01-图标右键菜单.png`（真实页面截屏，图标右键弹出三项目菜单）、`02-宿主侧验证-启动置前与名称解析.txt`（T1–T4 全 PASS，含 `appZ=0 < novaZ=1` 的 z 序证据与 `logonui=1` 说明锁屏下仍可置前）、`03-页面侧验证-右键菜单交互.txt`（R1–R4 全 PASS，含「第一次点击不删数据」安全断言）、`04-页面诊断-URL应为根路径.txt`（页面挂在 `/` 而非 `/nova-launcher.html`） |
| 2026-09-12 18:35:00 | Tony | `D:\Study\AICode\WindowsEXE\.temp\win-probe.ps1` | 新增 CDP 验证配套探针：输出 JSON 形式的 `appHwnd/appZ/appTitle/novaHwnd/novaZ/fgHwnd/fgTitle/fgProc/visibleTitled/logonui`，用 **EnumWindows 的 z 序下标**（0 = 最上层）客观判定「应用是否在 launcher 之前」，不依赖抓屏、不受锁屏影响 |
| 2026-09-12 18:35:00 | Tony | `C:\Users\tonyy\.workbuddy\skills\windows-html-native-launcher\SKILL.md` | 新增 §15「启动的程序要显示在最前」与 §16「拖入文件的绝对路径拿不到」：前者给出「z 序（TOPMOST→NOTOPMOST，无需权限、必生效）与键盘焦点（受前台锁限制）分两层」的做法、快照差集找新窗口、目标选取启发式；后者给出「浏览器只给文件名 → 宿主在桌面/开始菜单建索引解析」的方案与索引缓存；另补三条通用坑：PS 5.1 `Add-Type` 是老编译器不支持 C#7 内联 `out`、不要读 `$env:APPDATA`/`$env:USERPROFILE`（某些环境下为空）而应用 `[Environment]::GetFolderPath()`、`@(pipeline \| ConvertFrom-Json)` 在 PS 5.1 会得到「1 个元素」的数组、capture 阶段 `stopPropagation` 会让子元素监听器收不到事件、跑 `agent-browser` 会连带清掉已有浏览器进程 |
| 2026-09-12 18:37:00 | Tony | `D:\Study\AICode\WindowsEXE\README.md` | **新建仓库根 README（GitHub 门面）**：项目徽章（platform/runtime/dependencies/绿色版/storage 五枚静态 shields）、一句话定位与「只做 5 件事」的产品主张、ASCII 界面示意（不依赖未入库的截图）、快速开始（下载 ZIP 双击 `.bat` / git clone 两条路径）、使用说明（三种添加方式 / 图标右键菜单 / 排序与外观 / 窗口三键）、工作原理图与关键设计表（同源托管、token 鉴权、25s 心跳自退、BrowserProfile 隔离、置前分 z 序与焦点两层、图标按路径绑定、拖入按名解析）、目录结构、数据存放、技术选型表、**「为什么是 HTML + 宿主而不是 WinUI 3」**（本机无 .NET SDK 无法编译 WinUI 3，原设计降级为 V2.0 蓝图）、里程碑 M0–M20 汇总表、文档索引（8 项相对链接已逐个校验存在）、开发约定（源码 BOM/CRLF 要求、临时产物不入库、中文 commit 规范）、FAQ 7 条、许可说明（未附 LICENSE，需补后再发布）。与 `NovaLauncher.Web\README.md`（使用向详细说明）形成「仓库总览 / 使用手册」两级分工 |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M21 宿主侧**：新增 `^/api/rename$` 路由 —— 重命名**只改 apps.json 里的显示名，不动磁盘上的快捷方式文件**（与 /api/remove 同一原则）；定位**优先用 `p`（路径稳定）**、拿不到再退回 `i`（行号随拖拽变化不作主键）；空名（trim 后为空）拒绝并提示「名称不能为空」、名称截 60 字符、缺 token 403；同名幂等返回不重写落盘；头注释块补记 M21/M22 修改摘要与时间戳 |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M21 前端侧**：右键菜单新增「**重命名**」项，菜单**原地变身**成「输入框 + 确定/取消」（与移除的二次确认同模式，不弹系统对话框）：回车确定、Esc/取消回菜单态；输入框 `keydown` 必须 `stopPropagation()`（否则 Esc 命中全局 keydown 直接关菜单）；空名就地提示「名称不能为空」；自动聚焦+全选（`.open` 后 `setTimeout(0)` 再 focus）；`buildCtx` 由布尔参数重构为 `mode`（normal/confirmRemove/edit）；新增 `renameApp()`（就地改 `state.apps[i].name` 后同时重绘网格与设置抽屉列表）与 `API.rename(i,p,name)`、演示模式 `demoRename()`；磁贴 title 提示补「重命名」 |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M22 前端侧**：`<head>` 新增 `<link rel="icon" href="nova-logo.ico?v=1">` —— `--app` 模式下窗口/任务栏/Alt-Tab 图标来自页面 favicon（此前一直 404、显示浏览器默认图标）；`?v=` 备好日后改图标的缓存钥匙；相对路径写法使 file:// 演示模式同样生效 |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M22 宿主侧**：`/favicon.ico` 与 `/nova-logo.ico` 由 404 改为**免 token** 托管 `nova-logo.ico`（`image/x-icon`）—— 图标无敏感信息，token 化反而让 `<link>` 写法复杂化；路径区新增 `$LogoIco` |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-logo.ico`、`nova-logo-256.png` | **新建应用图标**（与顶栏 `.brand .mark` 同款：135° 渐变 `#6d8bff→#9b7cff`、30% 圆角方块、白色四角星 ✦、四角透明）：ICO 41,345 B 含 5 尺寸，**256px 条目用 PNG 压缩 + 16/32/48/64 用经典 32bpp BMP（DIB）** 混合格式（全 PNG 条目 GDI+ `Icon.ToBitmap` 解不了，实测报「请求的范围扩展超过了数组的结尾」）；PNG 为无头 Edge 矢量直出（256）+ GDI+ HighQualityBicubic 缩放（其余）；打包用 node 自写 PNG 解码 + DIB 编码，零依赖（脚本存 `.temp\ico-build.mjs`） |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M21 里程碑**（T21.1–T21.5：/api/rename、菜单原地编辑态、Esc/聚焦细节、演示模式、API 11 项 + CDP 13 项双层验证）与 **M22 里程碑**（T22.1–T22.6：SVG 复刻 Logo、无头渲染视口/DPI 陷阱、混合格式 ICO、favicon 免 token 托管、6 项托管验证）；WBS 汇总、里程碑看板（M21/M22 行）、风险表新增 2 条（ICO 全 PNG 条目 GDI+ 解不了、无头截图视口陷阱）、交付物清单更新体积并补 nova-logo 行 |
| 2026-09-12 19:45:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md`、`D:\Study\AICode\WindowsEXE\README.md` | 两级 README 同步：Web 版「怎么用」第 7 条与「界面细节」补重命名（含「只改显示名不动磁盘文件」）、文件说明表补 nova-logo.ico/png；根版「图标右键菜单」与里程碑表补 M21/M22、目录结构补图标文件 |
| 2026-09-12 19:58:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M23 启动图标预热（宿主侧）**：缓存键抽为 `Get-IconKey` 公共函数；新增 `Resolve-LinkTarget`（`.lnk` 本体非 PE 提不出图标 → 解析实际目标再提，`/api/icon` 即席请求同样受益）；新增 `New-IconPreheatQueue`（启动扫 apps.json × {256, 网格 iconSize}，跳过 shell:/失效路径，Icons 已有则跳过）与 `Process-IconPreheat`（主循环空闲节拍每 1.5s 最多补 6 张，真实请求随时插队，清空记日志）；实测删 1 张 256 + 全新 100px 共缺 38 张全部补齐（Icons 227→265）、临时 lnk 返回 200+有效 PNG 并留「已改从目标提取」日志 |
| 2026-09-12 19:58:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M23 里程碑**（T23.1–T23.5）；WBS 汇总、里程碑看板补 M23 行；交付物清单宿主体积 86618→90700 B 并补预热描述 |
| 2026-09-12 19:58:00 | Tony | `D:\Study\AICode\WindowsEXE\README.md`、`D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 两级 README 同步：工作原理图与工作方式补「启动时 Icons 缺失即到应用实际位置补提、`.lnk` 解析目标兜底、空闲节拍分批不阻塞」 |
| 2026-09-12 20:10:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M24 宿主侧**：新增 `^/api/checkapps` 路由——逐项验证已添加应用有效性（路径为空 / 文件不存在 / `.lnk` 经 `Resolve-LinkTarget` 深挖目标，目标被卸载也判失效；`shell:` 跳过），返回 `{ total, okCount, bad:[{i,name,path,reason}] }` |
| 2026-09-12 20:10:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M24 前端侧**：设置抽屉「我的应用」新增「🩺 检查应用有效性」按钮 + 失效项结果面板（红边卡片：名称/原因/路径 + 单独「移除」 + 多于 1 项时「全部移除(N)」倒序删）；检查结果回写 `exists` 让网格与抽屉列表即时标红；移除后重跑检查规避行号位移；新增 `API.checkApps`、`runAppCheck`、`renderChkPanel` 及 `.chk-result` 系列样式 |
| 2026-09-12 20:10:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M24 里程碑**（T24.1–T24.5）；WBS 汇总、里程碑看板补 M24 行；交付物清单宿主体积 90700→92276 B |
| 2026-09-12 20:10:00 | Tony | `D:\Study\AICode\WindowsEXE\README.md`、`D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 两级 README 同步：Web 版「怎么用」第 8 条与根版「其他交互」补一键体检说明、根版里程碑表补 M23/M24 |
| 2026-09-12 20:14:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M25 设置抽屉区块重排**：外观（图标大小/每行图标数/主题）移到抽屉最上方，依次为 外观 → 我的应用 → 数据；纯 DOM 顺序调整，id 与事件绑定不变、零逻辑改动（74,099 B）；顺带修正头注释里 M24 摘要的编号笔误 |
| 2026-09-12 20:14:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M25 里程碑**（T25.1）；WBS 汇总、里程碑看板补 M25 行；交付物清单界面体积 68583→74099 B 并补 M24/M25 功能描述 |
| 2026-09-12 20:25:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-logo.ico`、`nova-logo-256.png` | **M26 重制应用图标（修复「只有上半块」）**：根因=无头 Edge 渲染的源 PNG 下半部大面积透明（上1/4 透明 4402 / 下1/4 16128，应对称），ICO 忠实打包了残缺源图；改为 node 逐像素光栅化（135° 渐变 + 30% 圆角 SDF + 四角星多边形 + 2x2 超采样），5 尺寸上/下半透明差 0.0% 全 PASS、星心纯白、四角透明；重建混合格式 ICO（41345→36254 B）并复检 BMP 条目掩码上下对称；GDI+ 全条目解析通过 |
| 2026-09-12 20:25:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | favicon 链接 `?v=1`→`?v=2` 破 BrowserProfile 的 favicon 缓存（否则看不到新图标）；头注释补 M26 摘要（74,392 B） |
| 2026-09-12 20:25:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M26 里程碑**（T26.1–T26.5，含根因分析）；WBS 汇总、里程碑看板补 M26 行；交付物清单更新图标与界面体积 |
| 2026-09-12 21:52:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M27 窗口三键不依赖宿主**：关闭键在 /api/win 失败时立即降级为浏览器原生 window.close()；新增 host-dead 降级态（心跳确认宿主退出或窗口操作失败时进入），最小化/最大化置灰并给出明确提示，不再弹「窗口操作失败」；E2E（--app Chrome + CDP）6/6 PASS |
| 2026-09-12 21:52:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M27 里程碑**（T27.1–T27.4，含平台边界说明）；WBS 汇总、里程碑看板补 M27 行；交付物清单界面体积 74392→80579 B 并补三键降级描述 |
| 2026-09-12 21:52:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 「无边框最大化」一节补三键降级行为说明（关闭键不依赖宿主 / 降级态置灰与提示） |
| 2026-09-12 21:52:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\nova-launcher.html` | **M27 窗口三键不依赖宿主**：关闭键在 /api/win 失败时立即降级为浏览器原生 window.close()；新增 host-dead 降级态（心跳确认宿主退出或窗口操作失败时进入），最小化/最大化置灰并给出明确提示，不再弹「窗口操作失败」；E2E（--app Chrome + CDP）6/6 PASS |
| 2026-09-12 21:52:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M27 里程碑**（T27.1–T27.4，含平台边界说明）；WBS 汇总、里程碑看板补 M27 行；交付物清单界面体积 74392→80579 B 并补三键降级描述 |
| 2026-09-12 21:52:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 「无边框最大化」一节补三键降级行为说明（关闭键不依赖宿主 / 降级态置灰与提示） |
| 2026-09-12 22:50:00 | Tony | `D:\Study\AICode\WindowsEXE\NovaLauncher.Web\NovaLauncher.ps1` | **M28 任务栏图标修复（宿主侧）**：C# 增补 `LoadImage`/`WM_SETICON`（大图标=任务栏、小图标=标题栏）、`SHGetPropertyStoreForWindow` 写窗口 AUMID、ShellLink QI `IPropertyStore` 写 .lnk 的 `PKEY_AppUserModel_ID`；新增 `Set-NovaWindowIcon` 四步流程 = 幂等重建开始菜单 `Nova Launcher.lnk`（目标=NovaLauncher.bat、图标=nova-logo.ico、AUMID=Nova.Launcher）→ 设窗口 AUMID → `WS_EX_TOOLWINDOW` 短暂切换强制任务栏重建按钮 → WM_SETICON；`WM_GETICON` 读回比对盯守（浏览器 favicon 解析完会覆盖回，不一致即重设，连续 8 拍稳定收工、上限 40 次）；挂入主循环空闲节拍。排障实录：任务栏按 AUMID 取图标不看窗口图标；按钮在窗口显示时就定型；`SHGetPropertyStoreFromParsingName` 对 lnk AUMID 是只读视图（SetValue 静默 S_FALSE）；`IPersistFile::Load(path,0)` 只读打开写属性必报 0x80030005（须 0x42 读写）；WScript.Shell 写完须显式 ReleaseComObject 否则文件被持有；UIA 验证按钮由「Google Chrome - 1 个运行窗口」变为「Nova Launcher - 1 个运行窗口」（92276→107264 B，语法 0 错误） |
| 2026-09-12 22:50:00 | Tony | `D:\Study\AICode\WindowsEXE\DOCS\04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M28 里程碑**（T28.1–T28.4，含三层根因分析与六条排障实录）；WBS 汇总、里程碑看板补 M28 行；交付物清单更新宿主体积 92276→107264 B 与任务栏图标三件套描述 |
| 2026-09-12 22:50:00 | Tony | `D:\Study\AICode\WindowsEXE\README.md`、`D:\Study\AICode\WindowsEXE\NovaLauncher.Web\README.md` | 两级 README 同步 M28：任务栏/Alt-Tab 图标修复说明、开始菜单自动生成 `Nova Launcher.lnk`（可固定到任务栏）、里程碑表补 M28 |
| 2026-09-12 23:10:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//`（目录重组 M29） | **M29 目录重组**：根目录只保留 `NovaLauncher.bat` 唯一入口，其余 12 项（NovaLauncher.ps1 / nova-launcher.html / nova-logo.ico / nova-logo-256.png / README.md / apps.json / settings.json / Icons\ / BrowserProfile\ / host.log / runtime.json / NovaLauncher.lnk）全部移入 `data\`。改动 3 处：① bat `-File` 指向 `data\NovaLauncher.ps1`；② ps1 内开始菜单 lnk 目标改指上级目录 bat（`Split-Path $ScriptDir -Parent`）；③ `.gitignore` 6 条路径加 `data/` 前缀。宿主代码主体零改动（`$DataDir = $ScriptDir` 设计使数据路径自动跟随）。验证：语法 0 错误；完整启动全链路 OK（无边框最大化 / 图标预热补提 2 张 / lnk 重建 / AUMID / WM_SETICON 稳定）；HTTP 实测 `/api/installed` 274 项、`/favicon.ico` 36254 B、页面 72605 B |
| 2026-09-12 23:10:00 | Tony | `D://Study//AICode//WindowsEXE//README.md`、`D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//README.md` | 两级 README 同步 M29：目录结构图加 data\ 层级、数据存放表加 `data\` 前缀、排查/重置路径改为 data\ 下 |
| 2026-09-12 23:10:00 | Tony | `D://Study//AICode//WindowsEXE//DOCS//04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M29 里程碑**（T29.1–T29.4）；里程碑看板补 M29 行；交付物清单路径全部改为 `data\` 下并更新体积（bat 1234 B / ps1 107428 B） |
| 2026-09-13 00:05:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1`（108120 B） | **M30 窗口化也无系统标题栏**：① `Set-NovaWinMaximized -Windowed` 不再加回 WS_CAPTION/WS_THICKFRAME——窗口化改为无边框居中小窗 1180×780（页面顶栏与三键自然露出）；② `Get-NovaWinState` 由「有无 WS_CAPTION」改为窗口矩形与工作区矩形比较（含 csd 上移偏移，容差 2px）。前端零改动（state 语义不变）。Win32 实测往返：全屏→窗口化 1770×1170 居中无边框；窗口化→全屏顶部上移 59px 隐藏自绘栏；两态 WS_CAPTION/WS_THICKFRAME 均 False。排障：屏幕出现两个 Nova 窗口系 Chrome 从 BrowserProfile 恢复会话的孤儿窗口（非代码缺陷），已 WM_CLOSE 清理 |
| 2026-09-13 00:05:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//README.md`、`D://Study//AICode//WindowsEXE//DOCS//04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | README 更新最大化按钮说明（任何状态无系统标题栏）；DOCS04 新增 M30 里程碑（T30.1–T30.3）+ WBS + 看板行 |
| 2026-09-13 00:55:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1`（108862 B） | **M31 窗口化 CSD 残条修复**：主人实测「全屏→窗口化标题栏还是出现」。再排障：Win32 样式全程无 WS_CAPTION，PrintWindow 铁证显示那条灰条是 **Chromium 自绘标题条（CSD，「⚡ Nova Launcher ─ □ ✕」，高 46 物理px≈31 逻辑px，不随状态变）**——全屏靠「窗口上移 csd」顶出屏幕，窗口化居中时露出来。修复：C# 增补 `SetWindowRgn`/`CreateRectRgn`；`Set-NovaWinMaximized` 窗口化分支裁掉窗口顶部 32 逻辑px（31+1 余量）、全屏分支清除 region（上移方案下再裁会裁到页面）。验证：屏幕 BitBlt 抓屏（PrintWindow 无视 region 不能用于验证）——窗口化顶部 y=0 即页面色 (20,20,20)，往返切换两态均无标题条 |
| 2026-09-13 00:55:00 | Tony | `D://Study//AICode//WindowsEXE//DOCS//04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M31 里程碑**（T31.1–T31.3）+ WBS + 看板行 |
| 2026-09-13 01:30:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1`（113887 B） | **M32 窗口化切换后「下半屏未光栅化」修复（宿主侧）**：主人截图全屏→窗口化后底部大片纯色 (32,32,32)。排障（Win32 rect/CDP 布局指标/PrintWindow/连通域图标地标）：Win32 矩形精确居中、页面布局正确（视口 1167×745），坏帧是 Chromium **未光栅化瓦片滞留**——「原生窗口遮挡检测」(CalculateNativeWinOcclusion) 在窗口被盖住时暂停光栅化；RedrawWindow/合成 WM_SIZE/±1px 伸缩/前台化/最小化还原/DWM Cloak 六种手段实测**均无法**唤醒，只能禁用根因特性。修复：① 启动参数加 `--disable-features=CalculateNativeWinOcclusion`；② `Set-NovaWinMaximized` 的 `SetWindowPos` 补 `SWP_NOCOPYBITS`（丢弃旧位图）；③ 新增 `/api/win` 诊断操作 `rect`（窗口/客户区/工作区/页面上报尺寸）与 `pwshot`（PrintWindow 离屏抓帧，不受遮挡影响） |
| 2026-09-13 01:30:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//nova-launcher.html`（81250 B） | **M32（页面侧）**：resize 防抖 150ms 后对 body 做一次 `translateZ(0)` 开关，强制合成器整帧重绘，确保新尺寸下画面完整（配合宿主禁用遮挡检测双保险） |
| 2026-09-13 01:30:00 | Tony | `D://Study//AICode//WindowsEXE//DOCS//04Windows_Nova_Launcher_V1.0_HTML+宿主方案开发计划.md` | 新增 **M32 里程碑**（T32.1–T32.4，含根因分析与六种无效手段实录）；WBS 汇总、里程碑看板补 M32 行 |
| 2026-09-13 02:16:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1`（126067 B） | **M33 全屏露系统标题条 + 窗口化界面显示异常修复（宿主侧）**：winprobe 探针实测锁定五处缺陷——D1 最小化时 `SetWindowPos` 被 Windows restore-to-rcNormalPosition 覆盖 → 全屏意图落到旧窗口化几何且 region 已清 → CSD 整条暴露（Bug1 主因）；D2 窗口化 region 写死 32px vs 实际 CSD=28px 多削 4px；D3 外框未补偿「不可见缩放边框 fw=14/fh=7」→ 外框 1180×780 时页面可见仅 741px（Bug2 主因）；D4 CSD 推算单次竞态失败即永久锁死 0（host.log 实锤 -102/-95 两种）；D5 切换后 Chromium 重排滞后。修复：①`Set-NovaWinMaximized` 先 `IsIconic`→`SW_RESTORE`+150ms 再摆几何，两形态都按动态 csd+fw/fh 补偿并双保险设 region；②新增 `$script:WantWindowed` 意图记忆——最小化不是形态，还原必回意图形态；op=max 三分支语义修正；③`Update-NovaCsdHeight` 防竞态改「innerHeight\|外框高」双稳定指纹（连续两次相同才采样）+ 连败 8 次才放弃；④新增 `Invoke-NovaWindowGuard` 守门（空闲节拍+心跳复核样式位/几何 vs 意图，不符重摆）；⑤`Get-NovaWinState` 几何判定加 fw/fh 补偿、容差 2→8px；⑥`/api/ping` 回传 state；⑦新增诊断 op=front/op=hit（WindowFromPoint+GetAncestor(GA_ROOT) 命中测试，region 裁剪硬判据）。验证全 PASS：全屏 ih=912=work.h、topOffset=-csd、WS_CAPTION=False；窗口化 client 1180×810、可见页面 780；双向最小化→恢复形态保持；op=hit csd-zone 不命中/页面区命中；重启后 CSD 一次推算成功不再锁 0 |
| 2026-09-13 02:16:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//nova-launcher.html`（81736 B） | **M33（页面侧）**：`ping()` 解析宿主回传的 `state` 并同步 `applyWinChrome`——守门在背后纠正形态后，最大化/还原图标每次心跳自动跟上（`connOk()` 保持在 JSON 解析前调用，state 解析失败不误判宿主挂掉） |
| 2026-09-13 02:16:00 | Tony | `D://Study//AICode//WindowsEXE//.temp//winprobe.ps1` | **M33 验证探针（临时件）**：读 runtime.json → 调 `/api/win?op=rect` → 输出 state/bar/rect/client/work/ih/style/topOffset/fw/fh/visiblePageH + CHECK-fullscreen/windowed/no-caption 断言；用法 `& .temp\winprobe.ps1 -Label X -Op none\|max\|min` |
| 2026-09-13 03:10:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1` | **M34 CSD 偏移持久化 + reload 兜底回滚（宿主侧）**：① 新增 `data/csd.cache` 把实测 CSD 偏移落盘，启动瞬间即读到上次值→零暂态、不再首帧露标题条；② `Get-NovaCsd` 取值优先级「本次实测 > 缓存 > 兜底 34」；③ CSD 闸门收窄到 [16,48]，区间外判为 artifact 拒绝但**不锁 0**（锁 0 会永久露标题条）；④ 守门 `GuardStreak ≥4` 拍仍纠正不符则暂停自动重摆，防窗口无限抖动。**回滚**：上一轮试图用 `location.reload` 修「--app 视口不跟随 resize」——实测 reload 复用同一 RenderWidgetHostView 重建不了视口，且引发每 3s 重载风暴打断心跳（红点「未连接宿主」），已全部撤销 |
| 2026-09-13 04:20:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1` | **M35 界面只铺左上根因修复（DPI 感知、宿主侧）**：主人截图「进入软件后界面显示不完整、只铺左上角」。根因：PowerShell 5.1 宿主默认**不感知 DPI**，而 Chrome `--app` 是 PMv2 感知——宿主用逻辑坐标摆物理窗口，Chrome 光栅化表面按逻辑尺寸 1:1 呈现，dpr=1.5 时画面只占左上 2/3、右底大片空白。修复：宿主启动主动 `SetProcessDpiAwarenessContext(PMV2)`（失败退 `SetProcessDPIAware`），开启后全链路统一物理 px；新增 `$script:DpiScale`（系统 DPI/96，并用页面回传 dpr 校准）换算所有逻辑常量（窗口化 1180×780、CSD 闸门 [16,48]、兜底 csd 34）。**坐标空间变更须清 `csd.cache`**（旧缓存是逻辑 px 会错位）。验证：干净单次启动 + 全屏截图，界面铺满工作区、绿点/网格/三键正常 |
| 2026-09-13 10:30:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//nova-launcher.html`、`D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1` | **M36 窗口化拖动移窗（跟随鼠标）**：窗口化时按住自绘标题栏拖动可移动整窗并实时跟随鼠标。**方案选型**：先试 CSS `-webkit-app-region:drag`——实测**无效**（本项目 `--app` 窗被宿主从外部剥了 WS_CAPTION 又 `SetWindowRgn` 裁过顶栏，该属性既不触发系统 SC_MOVE 拖动、又会把标题栏变成非客户区吞掉 DOM 鼠标事件），遂弃用；改「页面 JS pointer 拖拽 + 宿主 `op=move`」：`pointerdown` 记鼠标/窗口基准 + `setPointerCapture`，`pointermove` 用 rAF 合帧 + in-flight 合并（在途只留最新点、绝不堆请求），位移 = CSS px × dpr 换物理 px，发 `/api/win?op=move&x&y`；宿主 `SetWindowPos`（SWP_NOSIZE / SWP_NOZORDER / SWP_NOACTIVATE）只平移不改尺寸/层级/焦点，localhost 单请求 ~1-3ms 跟手。仅 `body.is-windowed`（`applyWinChrome` 按心跳 state 增删）时启用，控件上不拖；拖动不改形态，守门只在形态/样式异常时重摆，不会把拖走的窗拉回。验证：`.temp/drag-test.ps1` 模拟真实鼠标（SetProcessDPIAware + mouse_event LEFTDOWN/分步 SetCursorPos/LEFTUP）拖 dx=280 dy=150，op=rect 前后 delta dl=280 dt=150 完全吻合 → PASS；drag-before/after.png 全屏截图目视窗体从居中左移到右下移 |
| 2026-09-13 11:00:00 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1` | **M36 附带：宿主代码注释精炼**：全文件 44 个函数逐一核对——① 去冗余：把跨块复述的「战争故事」（前台锁、CSD/DPI、reload 教训）压成要点（摆窗 windowed/maximized/CSD 裁剪块、守门 reload 墓碑、Bring-NewWindowToFront 头共 5 处）；② 补详细注释：为 15 个此前无头注释的 helper 各补一行契约说明（Write-Log / Write-Utf8NoBom / Read-Json / Get-Apps / Save-Apps / Get-Settings / Save-Settings / Get-IconKey / Get-IconBytes / Process-IconPreheat / Send-Bytes / Send-Text 组 / Add-AppPath / Launch-App / Set-NovaWindowIcon）；③ 新增 `op=move` 分支详注（为何弃用 app-region）。纯注释改动，`Parser::ParseFile` 语法 0 错误、UTF-8 BOM(239,187,191) 保持 |
| 2026-09-13 11:20:00 | Tony | `D://Study//AICode//WindowsEXE//README.md`、`D://Study//AICode//WindowsEXE//修改记录.md`、`D://Study//AICode//WindowsEXE//.agent//AGENTS.md`、`D://Study//AICode//WindowsEXE//.gitignore` | **版本号 V0.0.1 + 文档同步**：README 加 version 徽章 V0.0.1、里程碑表补 M29–M36、加「拖动移窗」说明、DOCS04 引用范围 M0–M20→M0–M32、push 命令 master→main+代理；补记 修改记录 M34/M35/M36；AGENTS 执行计划 M1-M35→M1-M36 并加 M36 拖拽条目；`.gitignore` 加运行期产物 `NovaLauncher.Web/data/csd.cache` |
| 2026-09-13 13:28:18 | Tony | `D://Study//AICode//WindowsEXE//NovaLauncher.Web//data//NovaLauncher.ps1` | **M37 窗口化顶部黑边修复**：主人截图窗口化时顶部出现黑边、NOVA 顶栏被推到下方。根因：`csd.cache = 42` 是错误值（DPI 切换 artifacts），实际 CSD 应在 28-32 范围；旧闸门 [16,48] 太松放行了 42 → `SetWindowRgn` 多裁 10-14px 产生黑边。修复：① CSD 闸门从 [16,48] 收紧到 [20,40]（仍覆盖正常波动，拒绝 42 这类异常）；② 启动时自动检测缓存值是否超出新闸门，超出则清除并强制重新实测；③ 已手动删除错误 csd.cache。头注释更新最后修改时间 |
| 2026-09-13 16:30:00 | Tony | D:\Study\AICode\NovaLauncher\ | **V0.0.2 发布**：项目目录扁平化（移除 NovaLauncher.Web 子目录），清理运行时临时文件与 BrowserProfile/Icons 缓存，新增 .gitignore，代码提交至 GitHub |
| 2026-09-14 10:30:00 | Tony | D:\Study\AICode\NovaLauncher\Source\ | **M40 架构重构 + V0.0.3 发布**：彻底解决窗口化顶部黑边与点击穿透问题。架构从 Chrome --app + HttpListener + SetWindowRgn 迁移到 WinForms Form(None) + CoreWebView2Controller。关键变更：①渲染层改用 WebView2（Edge Chromium），嵌入 WinForms 无边框窗口，彻底消除 Chromium 自绘标题栏（CSD）导致的黑边；②通信层从 HttpListener REST API 改为 postMessage 双向通信，16个 op 全部迁移；③DPI 采用 Per-Monitor V2 + GetDpiForWindow + WM_DPICHANGED，多显示器正确；④初始化采用 Form.Shown + Timer 异步轮询，避免 UI 线程死锁；⑤毛玻璃主题：DWM Mica (Win11) + HTML 半透明背景 + backdrop-filter；⑥窗口化垂直居中 1180×780；⑦图标异步 base64 dataUri 加载。修复4个崩溃：DpiChanged命名冲突、BackColor不支持Transparent、SetCompatibleTextRenderingDefault顺序、iconData方法缺闭合括号。修复2个功能bug：Get-Settings遗漏win属性、HOSTED检测从protocol改为chrome.webview存在性。项目结构调整：所有源代码移入 Source\ 目录。清理无用文件（M39备份/PoC/缓存/旧文档）。发布绿色版 NovaLauncher-V0.0.3.zip (0.26MB) || 2026-09-16 11:18:14 | Tony | D:\Study\AICode\NovaLauncher\ | **V0.0.6 发布**：拖拽方案重构为 NovaDrop（OLE IDropTarget）——关闭 WebView2 AllowExternalDrop，在主窗口+全部子窗口 RegisterDragDrop，定时器重注册处理动态子窗口，支持任何文件/文件夹拖拽添加（记录绝对路径不创建.lnk），拖拽悬停显示「创建快捷方式」遮罩；指针事件实现应用图标拖拽排序；清理 verify/ 临时验证截图与 Release 旧解压目录；更新 README 版本号与版本历史；更新 .agent/AGENTS.md |

| 2026-09-16 13:46:55 | Tony | `D:\Study\AICode\NovaLauncher\Source\data\NovaLauncher.ps1` | **V0.0.7 背景模糊脱离 DWM 材质**：实测确认 DWM 的 Mica/亚克力会把窗口背景整块顶掉（背后放纯红窗口 220,30,30，透过任何 DWMSBT 材质红色分量均为 0），一旦启用透明度滑块即失效；旧版 accent API 在 Win11 也已退化为纯黑平层（0,0,0，无模糊）。故 Set-NovaDwmBackground 去掉 $bgBlur 参数与整个材质分支，模糊不再碰 DWM，窗口走纯透明框架让桌面真透出；6 处调用点同步；bgBlur 设置处理器改为纯存值 |
| 2026-09-16 13:46:55 | Tony | `D:\Study\AICode\NovaLauncher\Source\data\nova-launcher.html` | **V0.0.7 雾面膜与双击修复**：背景模糊改为前端「雾面膜」——新增 veil() 把基色按 blurK 向磨砂色插值（深色 16,16,20→58,62,74；浅色 238,240,245→206,210,220），**只改色调不碰 alpha**，不透明分支同样叠膜，两个滑块彻底解耦（实测：拖模糊 alpha 恒 0.200、拖透明度色调恒 58,62,74）；.brand 移出双击排除列表，修复标题栏 logo/应用名区双击无反应；模糊滑块加回 display:none；抽屉副标题版本号 V0.0.5→V0.0.7（两处） |
| 2026-09-16 13:46:55 | Tony | `D:\Study\AICode\NovaLauncher\README.md` | 标题版本号 V0.0.6→V0.0.7，版本历史新增 V0.0.7 条目（雾面膜重构、透明度解耦、双击 .brand 修复、滑块隐藏、旧 accent API 停用） |
